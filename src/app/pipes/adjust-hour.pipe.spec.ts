import { AdjustHourPipe } from './adjust-hour.pipe';

describe('AdjustHourPipe', () => {
  it('create an instance', () => {
    const pipe = new AdjustHourPipe();
    expect(pipe).toBeTruthy();
  });
});
